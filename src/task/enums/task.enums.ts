export enum Category {
  Scheduling = 'scheduling',
  Finance = 'finance',
  Technical = 'technical',
  Safety = 'safety',
  General = 'general',
}

export enum Priority {
  High = 'high',
  Medium = 'medium',
  Low = 'low',
}

export enum Status {
  Pending = 'pending',
  InProgress = 'in_progress',
  Completed = 'completed',
}
